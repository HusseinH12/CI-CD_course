name: first workflow
on:
  push:
    branches:
      - main
jobs:
  exmaple-job: #name of the job
    runs-on: ubuntu-latest
    steps:
      - name: print welcome message
        run: echo "welcome from github actions" #run for shell commands
      - name: checkout repo
        uses: actions/checkout@v6.0.2
      - name: list repo
        run: ls -la
    
