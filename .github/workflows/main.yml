on:
  repository_dispatch:
    types:
      - incident_report
jobs:
  handle_incident:
    runs-on: ubuntu-latest
    steps:
      - name: print incdient details
        run: echo "incident reported"
